[pytest]
# The canonical runner is scripts/run_tests.bat, which sets
# PYTHONPATH=file-agent before invoking pytest - this file bakes that in so
# a bare `pytest` (and Pi CI) works without it. testpaths=tests matches the
# runner exactly (helper scripts under scripts/ are never collected).
testpaths = tests
pythonpath = file-agent scripts
